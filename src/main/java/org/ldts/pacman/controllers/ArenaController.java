package org.ldts.pacman.controllers;

import org.ldts.pacman.models.Arena;

public class ArenaController extends Controller<Arena> {
    protected ArenaController(Arena model) {
        super(model);
    }
}
