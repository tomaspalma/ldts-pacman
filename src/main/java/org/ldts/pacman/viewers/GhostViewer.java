package org.ldts.pacman.viewers;

import org.ldts.pacman.gui.GUI;
import org.ldts.pacman.models.Ghost;

public class GhostViewer implements EntityViewer<Ghost> {
    @Override
    public void draw(Ghost element, GUI gui) {

    }
}
